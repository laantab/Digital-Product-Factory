# Production runtime — update deliberately and run preflight_check.py afterward.
Flask==3.1.1
Flask-Login==0.6.3
bcrypt==5.0.0
Markdown==3.8.2
Pillow==12.2.0
PyMuPDF==1.26.6
beautifulsoup4==4.13.4
# Cloudflare R2 object storage (S3-compatible API) — Upgrade 0, Phase 0B-3B1.
# The only cloud-storage client the Factory uses. Imported lazily, so the
# app runs normally without it until FACTORY_STORAGE_DRIVER=r2 is set.
boto3==1.40.15
openai==1.97.1
# PostgreSQL driver — Upgrade 0, Phase 0B-4. Imported lazily and used only
# when FACTORY_DB_BACKEND=postgres AND DATABASE_URL are both set; with
# either absent the Factory runs on SQLite exactly as before.
psycopg[binary]==3.2.9
python-dotenv==1.1.1
# Render Workflows SDK — v1.8.0. Imported lazily and used only when
# FACTORY_EXECUTION_MODE=workflow: the web service imports it inside the
# trigger function, and only the workflow service imports it at startup.
# With the variable unset the Factory runs exactly as v1.7.29 did, and a
# local Windows checkout never needs a Render account.
render==1.1.0
pypdf==6.10.0
reportlab==4.4.9
requests==2.32.4
svglib==1.5.1
tavily-python==0.7.10
xhtml2pdf==0.2.17
youtube-transcript-api==0.6.2
