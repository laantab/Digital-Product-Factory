# RESUME HERE — 2026-09-25

Written by a Claude Code **cloud** session (Linux container, host `vm`).
It is NOT the owner's Windows PC. This file exists only in that container
and was not committed or pushed.

## Verified

- Checkout HEAD = `origin/main` = `32fdd1d` (Factory 1.9.5, the merge of `laantab/Digital-Product-Factory#22`). Working tree clean.
- `VERSION` = 1.9.5.
- The fast Stability Gate at `32fdd1d` (Linux, Python 3.11) passed: 700 passed, 0 failed, 0 paid calls.
- GitHub: no checkpoint branch dated 2026-09-24 or later. Four older branches are ahead of `main`
  (billing-v1.3.0, claude/domain-setup-progress-klt1fr,
  cursor/customer-final-zip-promotion-8d87, onedrive-workspace-phase-a).
  The two open pull requests (`laantab/Digital-Product-Factory#1` and
  `laantab/Digital-Product-Factory#2`) are old drafts, not part of this work.
- `FACTORY_HANDOFF_2026-09-24.md` and `RESUME_HERE` were never committed, so they exist only on the PC.

## Not verified (blocked)

- The live website version, the Render deployment, and the builder's version. This environment's network
  policy denies digitalproductfactorypro.com, digital-product-factory.onrender.com and
  api.render.com.
- The PC's local tree, its running servers, `projects.db`, and `exports/`.
- Container Gardening for Beginners: PDF, ZIP, both downloads, Saved Projects entry, reopening without a rebuild.
- Spending, and whether any builder runs are active.

No action was taken that could start a build. Nothing was spent.

## Remaining blocker

This work must run on the Windows PC (Claude Code opened in
`C:\Users\user\Documents\Product-Pipeline\Factory-v1.3`), or this cloud
environment's allowed domains must include the three hosts above, plus a
signed-in session for the Saved Projects check.
